# Публичные страницы Meta — 17 сентября 2026

Релиз `/opt/kontent-zavod/releases/contacts-20260917-v1` развёрнут на прежнем VPS
217.60.183.146. Предыдущий релиз `journey-20260916-v2` сохранён для отката.

На четырёх публичных страницах `/integration-info/overview`, `privacy`, `terms`
и `deletion` размещены предоставленные владельцем имя Mishgun Carter и контакт
ligsmish111@outlook.com. Инструкция удаления направляет запросы оператору.
Уточнены различия между отключением доступа, остановкой сбора, архивированием
канала и полным удалением; сроки хранения описаны по фактической реализации.

Production-сборка прошла через build-site.mjs. Контрольная сумма архива на VPS
совпала с локальной. После переключения все три systemd-сервиса active;
локальная HTTP-проверка подтвердила новые контактные данные. Env-файлы
не изменялись. Миграции и повторный сброс каналов не выполнялись.

Это обновление публичных страниц само по себе не включает API Instagram/Threads.
Кабинет Meta и выдача разрешений на момент этого деплоя ещё настраиваются.
TikTok исключён владельцем из текущей настройки.

В существующий nginx TLS server добавлен стандартный порт 443 для того же
hostname. Сертификат уже покрывал это имя; прежний порт 21443 сохранён.
На обоих портах проверены публичные страницы (200), защита админки и данных
(401), запрет внешнего доступа к sync/telegram (403). Конфигурация nginx прошла
проверку перед reload. Env и база при этом не изменялись.

Для проверки публичных страниц в Meta добавлен общедоступный robots.txt,
разрешающий её crawler, и снят X-Robots-Tag только с публичных инструкций.
Приватные формы и служебные маршруты сохраняют прежнюю защиту.
Meta получила новый robots.txt с HTTP 200, но на момент проверки её Sharing
Debugger продолжает показывать 403. Серверные логи не подтверждают новых
запросов Meta к странице удаления после обновления robots.txt; причина
оставшейся ошибки пока не установлена.

## Обработчики Meta

Релиз `/opt/kontent-zavod/releases/meta-callbacks-20260917-v1` развёрнут
17 сентября в 16:26 MSK. SHA256 архива:
`e44d9231740447219e8da7362eed976ea3320304fcb697602ea7c478740fe66e`.
Предыдущий `contacts-20260917-v1` сохранён. Все три сервиса active/running,
NRestarts=0. Env не менялся, CONTENT_PUBLIC_ORIGIN пока с портом :21443.

Добавлены отдельные POST-маршруты deauthorize и data-deletion для Instagram
и Threads. Проверяются подпись продукта, идентификатор аккаунта и время события.
Удаление ограничено подтверждёнными подключениями соответствующего продукта;
старое событие не затрагивает новое подключение. Частная квитанция описывает
фактический объём удаления, без обещания автоматического удаления резервных копий.

После остановки app/sync/telegram выполнена аддитивная миграция 0012.
Проверенная закрытая копия базы:
`/var/lib/kontent-zavod/backups/before-meta-callbacks-20260917-v1.sqlite`.
Количество существующих каналов, креаторов, Telegram-аккаунтов и подключений
проверено мигратором и сохранено. Повторного сброса не было.

162 теста, TypeScript и lint прошли; production-сборка завершилась успешно.
nginx проверен и перезагружен. Снаружи подтверждены public pages 200,
админка/data 401, sync/telegram 403, неизвестная квитанция из 64 hex 404.
POST callbacks дают ожидаемый 503 до установки отсутствующих product credentials.
Это проверка закрытого состояния, а не подтверждение рабочего Meta OAuth.

В кабинете Meta сохранены все три адреса Instagram и Threads. Для Threads
сохранение проверено перезагрузкой, для Instagram получено сообщение об успехе.
Meta по-прежнему отклоняет URL инструкции удаления в общих настройках.
Секреты продуктов не получены: повторное подтверждение Facebook-пароля не прошло.
Приложение не опубликовано; подключение реального креатора и App Review впереди.


## YouTube: рабочий общий ключ и личная форма

Релиз `/opt/kontent-zavod/releases/youtube-20260917-v1` развёрнут
на том же VPS 217.60.183.146. Предыдущий `meta-callbacks-20260917-v1`
сохранён для отката. SHA256 поставленного архива:
`5d8d3e47b9eaa41e2d93c517da82b06065a2bb8df4ba9fce287c1bdba360d71f`.

В Google Cloud, проект kontent-zavod-508809, создан ключ
Kontent Zavod YouTube VPS. Ограничен YouTube Data API v3 и IP VPS.
Сохранён только в серверном sync.env с прежними остальными настройками,
перед изменением создана закрытая резервная копия. Значение ключа
не включалось в исходники, архив релиза, сообщения или отчёт.

Бот после добавления YouTube предлагает «Проверить сбор». Личный ключ
остаётся необязательным. Если Google API key прислали текстом, в подписи
или скрытой ссылке, бот предлагает форму своего канала и просит удалить
сообщение с ключом. Такой текст не сохраняется как название или URL канала
и не передаётся в ответы. Личный ключ вводится в одноразовой форме на 10 минут.
Обновлены подпись поля, пояснения и инструкции, выдаваемые TXT-файлами.

Сборщик пробует личный ключ, а при его ошибке — общий ключ сервиса.
Сохраняет привязку к channel ID; личная ошибка авторизации отмечается
отдельно от результата сбора. Квота не помечает рабочий ключ недействительным.
Лайки суммируются по полному доступному публичному каталогу; неполная
сумма не выдаётся за полную. Закрытая аналитика не подключалась.

Проверки: 179 тестов, lint, TypeScript и production-сборка прошли.
Контрольная сумма архива на VPS совпала, три сервиса active.
Миграций и сброса данных не было. Публичная /api-guide вернула HTTP 200.
Реальный запрос YouTube с VPS для существующего канала 182 успешен:
101504 просмотра, 24 видео, 1515 лайков, 51 подписчик на момент проверки.
Через внутренний recheckChannel в очередь поставлены четыре существующих
YouTube-канала: 182, 186, 191 и 199. Сообщения пользователям не отправлялись.

Повторный фоновый сбор завершён для всех четырёх каналов:

| Канал ID | Просмотры | Видео | Лайки |
| --- | ---: | ---: | ---: |
| 182 | 101504 | 24 | 1515 |
| 186 | 17189 | 24 | 145 |
| 191 | 32628 | 24 | 389 |
| 199 | 0 | 3 | 46 |

У всех sync_status=success, sync_source=youtube-data-api-full-public,
consecutive_failures=0 и lease_until=NULL. Записи истории соответствуют
текущим показателям. Значения — ответ API на момент проверки; отсутствие
данных не заменялось нулями. Все три сервиса active/running, NRestarts=0.
