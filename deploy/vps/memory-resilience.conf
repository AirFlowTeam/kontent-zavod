[Service]
# Avoid indefinite memory.high throttling. The hard cap still protects the host;
# systemd restarts the entire app group if the cap is exhausted.
MemoryHigh=infinity
MemoryMax=1G
OOMPolicy=kill
Restart=on-failure
